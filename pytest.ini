[pytest]
; This machine's default AppData\Local\Temp\pytest-of-<user> is not writable,
; so tmp_path/tmp_path_factory fixtures fail with PermissionError. Use a
; local, always-writable directory instead.
addopts = --basetemp=.pytest_tmp
